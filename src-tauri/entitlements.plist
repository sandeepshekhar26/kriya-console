<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hardened-runtime entitlements for the notarized Console. Kept minimal; add more only if
       notarization or runtime actually requires it.
       - disable-library-validation: lets the .app launch the separately-built kriya-gateway sidecar
         (signed with the same identity but a different team-less build) without a library-validation kill.
       - network.client: the gateway makes outbound connections (MCP downstreams, on-prem kriyad mTLS). -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
