# Rust build output
/target

# Tauri-generated schemas / capabilities cache
/gen/schemas

# The bundled gateway sidecar is built by scripts/bundle-gateway.sh, not committed (it's a 4.5MB
# binary produced from the public repo). Run that script before `tauri build`.
/binaries
